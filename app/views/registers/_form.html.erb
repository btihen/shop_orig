<%= form_with(model: register, local: true) do |form| %>
  <% if register.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(register.errors.count, "error") %> prohibited this register from being saved:</h2>

      <ul>
      <% register.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start_amount %>
    <%= form.text_field :start_amount, id: :register_start_amount %>
  </div>

  <div class="field">
    <%= form.label :close_amount %>
    <%= form.text_field :close_amount, id: :register_close_amount %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :register_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
