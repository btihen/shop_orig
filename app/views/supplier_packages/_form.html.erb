<%= form_with(model: supplier_package, local: true) do |form| %>
  <% if supplier_package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supplier_package.errors.count, "error") %> prohibited this supplier_package from being saved:</h2>

      <ul>
      <% supplier_package.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :supplier_package_product_id %>
  </div>

  <div class="field">
    <%= form.label :included_product_id %>
    <%= form.text_field :included_product_id, id: :supplier_package_included_product_id %>
  </div>

  <div class="field">
    <%= form.label :included_quantity %>
    <%= form.number_field :included_quantity, id: :supplier_package_included_quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
