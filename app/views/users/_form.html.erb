<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username, id: :user_username %>
  </div>

  <div class="field">
    <%= form.label :user_real_name %>
    <%= form.text_field :user_real_name, id: :user_user_real_name %>
  </div>

  <div class="field">
    <%= form.label :user_role %>
    <%= form.text_field :user_role, id: :user_user_role %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
