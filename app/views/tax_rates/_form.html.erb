<%= form_with(model: tax_rate, local: true) do |form| %>
  <% if tax_rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tax_rate.errors.count, "error") %> prohibited this tax_rate from being saved:</h2>

      <ul>
      <% tax_rate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tax_rate %>
    <%= form.text_field :tax_rate %>
  </div>

  <div class="field">
    <%= form.label :tax_start_date %>
    <%= form.date_select :tax_start_date %>
  </div>

  <div class="field">
    <%= form.label :tax_end_date %>
    <%= form.text_field :tax_end_date %>
  </div>

  <div class="field">
    <%= form.label :tax_category_id %>
    <%= form.text_field :tax_category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
