<%= form_with(model: order_line, local: true) do |form| %>
  <% if order_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_line.errors.count, "error") %> prohibited this order_line from being saved:</h2>

      <ul>
      <% order_line.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_line_quantity %>
    <%= form.number_field :order_line_quantity, id: :order_line_order_line_quantity %>
  </div>

  <div class="field">
    <%= form.label :order_line_note %>
    <%= form.text_area :order_line_note, id: :order_line_order_line_note %>
  </div>

  <div class="field">
    <%= form.label :order_line_actual_purchase_price %>
    <%= form.text_field :order_line_actual_purchase_price, id: :order_line_order_line_actual_purchase_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
