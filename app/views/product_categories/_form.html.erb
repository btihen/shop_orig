<%= form_with(model: product_category, local: true) do |form| %>
  <% if product_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_category.errors.count, "error") %> prohibited this product_category from being saved:</h2>

      <ul>
      <% product_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_category_name %>
    <%= form.text_field :product_category_name, id: :product_category_product_category_name %>
  </div>

  <div class="field">
    <%= form.label :tax_category_id %>
    <%= form.text_field :tax_category_id, id: :product_category_tax_category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
